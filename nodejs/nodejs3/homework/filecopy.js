const fs = require('fs');
var buf = Buffer.alloc(20480);


fs.readdir("./metal", (err, files) => {
    if(err) throw err;
    
    fs.readFile("./metal/", )

})
if("myfile" === "file")
fs.stat()
for(adsfadsf) {
    //읽고
    //filesize > buf.length 읽어
    //저장
    //console.log("*");
    //else myfile.restfilesize 읽어
    //저장
    //console.log("*");
    //쓰고
    // while문 사용, for는 +1 정도 할때
    // while은 특정조건에서 빠져나감
    // for는 간단한, 1,2차원
    // foreach,


}




const fs = require('fs');
var buf = Buffer.alloc(20480);


fs.readdir("./metal", (err, files) => {
    if(err) throw err;

    console.log("reading dir ! ! !")

    files.forEach((file) => {
        if(file === "metalxxx.avi") {
            file.
        }
    });

    fs.readFile("./metal/",(err, data) =>{
        if(err) throw err;
    ]
    });

    if()
});