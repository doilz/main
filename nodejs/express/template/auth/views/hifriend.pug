doctype html
html(lang="ko")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Simnply template PUG
    body 
        if(user)
            h1 Hi, #{user.name}
            h6 your age is, #{user.age}
        else 
            a(href="/dynamic") dynamic 
            